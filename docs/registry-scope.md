# Registry scope

The pinned registry is an identity source, not yet a declaration that every ISO entity is a sovereign country or that every provider supplies complete indicator coverage for it. Provider support and geography classification are validated separately before publication.
