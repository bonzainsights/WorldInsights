# Registry upgrades

Upgrading the pinned ISO source requires a reviewed dependency change and a regenerated compatibility report covering added, removed, renamed, and reassigned codes. Stable WorldInsights geography IDs must never be silently renumbered.
